// Minimal declarations for the raw-canvas generative-art module.
// The implementation lives in organism.js (plain ES classes).
export class MazeCanvas {
  constructor(...args: unknown[]);
}
export class PrimsOrganism {
  constructor(...args: unknown[]);
  animate(): void;
}
export class Worm {
  constructor(...args: unknown[]);
}
